import React from 'react';
import PropTypes from 'prop-types';

const Login = props => {
    return (
        <div id="login">
            
            
        </div>
    );
};

Login.propTypes = {
    
};

export default Login;