import React from 'react';
import PropTypes from 'prop-types';

const Board = props => {
    return (
        <div>
            
        </div>
    );
};

Board.propTypes = {
    
};

export default Board;